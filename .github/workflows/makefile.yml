name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install Autoconf
      run: sudo apt-get update && sudo apt-get install -y autoconf automake m4 libtool perl

    - name: Checkout chomik dependency
      uses: actions/checkout@v4
      with:
        repository: pawelbiernacki/chomik
        path: ./chomik

    - name: Build libchomik
      run: cd ./chomik && autoreconf -i && ./configure --enable-chomik-servers --enable-sdl-chomik && make

    - name: Install libchomik
      run: cd ./chomik && make install

    - name: Regenerate autoconf files
      run: autoreconf -i
      
    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Install
      run: sudo make install

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
